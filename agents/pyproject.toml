[project]
name = "captre-agents"
version = "0.1.0"
description = "Standalone agent world that demonstrates the Captre attestation platform"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "py-algorand-sdk>=2.11.1",
    "python-dotenv>=1.2.2",
    "rich>=14.0.0",
    "x402-avm[avm,fastapi]>=2.0.2",
]

[tool.uv]
package = false
